apiVersion: 1

providers:
  - name: 'Default Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      # This path MUST match the volume in docker-compose.yml
      path: /var/lib/grafana/dashboards